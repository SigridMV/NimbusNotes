// Data class representing a Note, containing an ID, title, and content
data class Note(
    val id: Int, // Unique identifier for each note
    val title: String, // Title of the note
    val content: String // Content of the note
)